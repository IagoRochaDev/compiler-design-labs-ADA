procedure main is
begin
end main;

function formula (base : Integer) return Integer is
  res : Integer;
begin
  res := not estaVazio + (calcular(base) * obterMultiplicador(ajustar(base)));
  return res;
end formula;
